{% extends 'base.html.twig' %}

{% block title %}Create alias{% endblock %}

{% block body %}
    {% include 'components/heading.html.twig' with { level: 1, content: 'Create alias' } %}

    <p class="mt-2 text-neutral-600">Create a new email alias under {{ alias_domain }}. No input needed—we'll generate one for you.</p>

    <form method="post" action="{{ path('app_alias_new') }}" class="mt-6" data-turbo="false">
        <input type="hidden" name="_token" value="{{ csrf_token('create_alias') }}">
        {% include 'components/button.html.twig' with { variant: 'primary', type: 'submit', label: 'Create alias' } %}
    </form>
{% endblock %}
